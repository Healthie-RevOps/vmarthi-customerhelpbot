# Pinned exactly. These are the versions the bot last answered questions on
# (July 2026). Unpinned ">=" ranges meant the Sep 14 rebuild silently pulled
# ~2 months of upgrades across every dependency at once and broke the message
# handler. Bump these deliberately, one at a time, never as a side effect of
# an unrelated deploy.
slack-bolt==1.30.0
slack-sdk==3.43.0
anthropic==0.83.0
beautifulsoup4==4.12.3
soupsieve==2.6
requests==2.32.3
gspread==6.1.4
google-auth==2.36.0
tzdata==2024.2
